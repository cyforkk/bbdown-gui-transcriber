# 功能总和文档

## BBDown 哔哩哔哩下载核心脚本（Python 跨平台版）

### 文件

- `download_bilibili_fav.py`
- `test_download_bilibili_fav.py`

### 功能说明

该脚本适用于 Windows、macOS、Linux。它支持两种下载来源：收藏夹批量下载和单个视频下载。

### 支持能力

- 支持从收藏夹链接自动解析 `fid`。
- 支持分页获取收藏夹全部视频。
- 支持单个视频链接或 BV 号下载。
- 支持下载模式二选一：`audio` / `video`。
- 支持 `--fav-url` 和 `--video-url` 互斥。
- 支持 `--bbdown-path` 指定 BBDown 可执行文件路径。
- 支持跨平台下载目录。
- 收藏夹批量下载时支持单个视频失败后跳过。
- 支持取消下载：停止后不再继续下载后续视频。
- 支持输出下载统计和停止状态。
- 使用 Python `unittest` 覆盖核心逻辑。

## BBDown 哔哩哔哩下载图形界面（Tkinter 版）

### 文件

- `bilibili_downloader_ui.py`
- `test_bilibili_downloader_ui.py`

### 功能说明

该文件提供 Tkinter 图形界面，作为现有 Python 下载脚本的 UI。它不包含 BBDown，只调用用户本机安装的 BBDown。

### 支持能力

- 支持选择下载来源：收藏夹链接或单个视频链接/BV 号。
- 支持输入链接或 BV 号。
- 支持选择下载类型：音频或视频。
- 支持选择下载目录。
- 支持自动检测 PATH 中的 `bbdown`。
- 支持手动选择 `bbdown.exe`。
- 支持开始下载和停止下载。
- 停止下载时会终止当前 BBDown 子进程，并阻止收藏夹继续下载下一个视频。
- 支持后台线程执行下载，避免 UI 卡死。
- 支持日志窗口实时显示 BBDown 输出。
- 支持根据系统首选编码读取 BBDown 输出，减少 Windows 中文日志乱码。
- 支持 Windows 下隐藏 BBDown 子进程控制台窗口，避免每个视频弹出 CMD。
- 支持下载完成后显示统计信息。

## exe 打包功能

### 文件

- `build_exe.py`
- `dist/BilibiliDownloaderUI.exe`
- `release/BilibiliDownloaderUI.exe`

### 功能说明

通过 PyInstaller 将 Tkinter GUI 打包为单文件 Windows exe，方便双击启动。

### 打包策略

exe 只包含 Tkinter GUI、Python 下载逻辑和项目脚本代码，不包含 BBDown。

### 输出结果

```text
D:\bbdown脚本\release\BilibiliDownloaderUI.exe
```

## 目录整理功能

### 处理结果

已清理可再生成的临时产物：

- `build/`
- `BilibiliDownloaderUI.spec`
- `__pycache__/`
- `*.pyc`

已新增发布目录：

- `release/BilibiliDownloaderUI.exe`

### 使用建议

普通用户使用 `release/BilibiliDownloaderUI.exe`。开发或重新打包时使用源码和 `build_exe.py`。
