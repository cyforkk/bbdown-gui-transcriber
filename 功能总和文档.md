# 功能总和文档

## uv 项目环境

### 文件

- `pyproject.toml`
- `uv.lock`
- `.python-version`
- `.gitignore`

### 功能说明

项目已从全局 Python 环境切换为 uv 管理的项目环境，便于迁移和复现。

### 依赖

- `faster-whisper`
- `nvidia-cublas-cu12`
- `nvidia-cudnn-cu12`
- `pyinstaller`（开发依赖）

### 使用方式

```bat
uv sync
uv run python bilibili_downloader_ui.py
```

## BBDown 下载核心脚本

### 文件

- `download_bilibili_fav.py`
- `test_download_bilibili_fav.py`

### 功能说明

支持收藏夹批量下载和单个视频下载，支持音频/视频二选一、指定 BBDown 路径、停止任务和下载统计。

## Faster-Whisper 转文字模块

### 文件

- `audio_transcriber.py`
- `test_audio_transcriber.py`

### 功能说明

从 `E:\faster-whisper` 项目抽取音频/视频转文字能力，封装为可被 GUI 调用的模块。

### 支持能力

- 支持 `.m4a`、`.mp3`、`.wav`、`.flac`、`.aac`、`.mp4`。
- 支持音频文件转文字。
- 支持 `.mp4` 视频转文字，识别的是视频中的音频流。
- 不支持识别视频画面文字，不做 OCR。
- 单文件转文字。
- 文件夹批量转文字。
- 批量转换时模型只加载一次，后续文件复用同一个 Faster-Whisper 模型实例。
- 输出同名 `.txt`。
- 加载 CUDA DLL 路径。
- 默认使用 `WhisperModel("medium", device="cuda", compute_type="int8_float16")`。
- 支持停止批量任务。
- 转录前使用 `ffprobe` 校验媒体文件和音频流。
- 对损坏媒体和空识别结果给出明确失败原因。

## Tkinter GUI

### 文件

- `bilibili_downloader_ui.py`
- `test_bilibili_downloader_ui.py`

### 功能说明

GUI 已整合下载功能和转文字功能。

### 支持能力

- 下载收藏夹或单个视频。
- 下载音频或视频。
- 停止下载任务。
- 单文件音频/视频转文字。
- 文件夹批量转文字。
- 停止转文字任务。
- 日志窗口实时输出。
- 隐藏 BBDown 子进程 CMD 窗口。
- 使用系统首选编码读取 BBDown 日志。

## exe 打包功能

### 文件

- `build_exe.py`
- `release/BilibiliDownloaderUI.exe`

### 当前状态

release exe 仍是下载器发布版。Faster-Whisper 转文字功能先通过源码 GUI 验证，暂不打包进 exe。
