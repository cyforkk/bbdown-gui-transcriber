# 功能总和文档

## BBDown 哔哩哔哩下载脚本（Python 跨平台版）

### 文件

- `download_bilibili_fav.py`
- `test_download_bilibili_fav.py`

### 当前维护策略

当前项目只保留并维护 Python 跨平台脚本，不再保留 PowerShell 版本。

### 功能说明

该脚本适用于 Windows、macOS、Linux。它支持两种下载来源：收藏夹批量下载和单个视频下载。

### 支持能力

- 支持从收藏夹链接自动解析 `fid`。
- 支持分页获取收藏夹全部视频。
- 支持单个视频链接或 BV 号下载。
- 支持下载模式二选一：
  - `audio`：只下载音频。
  - `video`：下载视频。
- 支持 `--fav-url` 和 `--video-url` 互斥，避免下载来源不明确。
- 支持跨平台下载目录：
  - Windows：`G:\默认收藏夹\音频`
  - macOS/Linux：`/home/user/downloads`
- 收藏夹批量下载时支持单个视频失败后跳过。
- 支持输出下载统计。
- 使用 Python `unittest` 覆盖核心逻辑。

### 收藏夹下载示例

```bash
python download_bilibili_fav.py \
  --fav-url "https://space.bilibili.com/619278616/favlist?fid=3928433616&ftype=create" \
  --mode audio \
  --output-dir "G:\默认收藏夹\音频"
```

### 单个视频下载示例

```bash
python download_bilibili_fav.py \
  --video-url "https://www.bilibili.com/video/BV1tkdVB4EpP/" \
  --mode audio \
  --output-dir "G:\默认收藏夹\音频"
```

也支持直接传 BV 号：

```bash
python download_bilibili_fav.py \
  --video-url "BV1tkdVB4EpP" \
  --mode video \
  --output-dir "G:\默认收藏夹\视频"
```

### 实现方式

1. 使用 `argparse` 接收命令行参数，并通过互斥参数组保证 `--fav-url` 和 `--video-url` 只能二选一。
2. 使用 `urllib.parse` 解析收藏夹链接中的 `fid`。
3. 使用正则表达式从单个视频链接或 BV 号中解析 `BV` 标识。
4. 使用 `urllib.request` 请求哔哩哔哩收藏夹 API。
5. 使用 `subprocess.run` 调用 `bbdown`。
6. 使用 `pathlib.Path` 创建和处理下载目录。
7. 使用 `dataclass` 表示视频、失败记录和下载结果。

### 测试覆盖

测试文件 `test_download_bilibili_fav.py` 覆盖：

- 收藏夹链接 `fid` 解析。
- 缺少 `fid` 时抛出明确错误。
- 单个视频链接 BV 号解析。
- 直接传入 BV 号解析。
- 单个视频链接缺少 BV 号时报错。
- 收藏夹下载和单个视频下载互斥。
- 音频模式 BBDown 参数构造。
- 视频模式 BBDown 参数构造。
- 收藏夹接口返回失败时给出未登录、私有或不存在提示。
- 批量下载时单个视频失败后跳过。
- 单个视频下载时使用解析出的 BV 号调用 BBDown。

### 测试命令

```bash
python test_download_bilibili_fav.py
```

## README 使用文档

### 文件

- `README.md`

### 功能说明

`README.md` 是项目使用说明文档，用于说明 BBDown 下载脚本的环境要求、使用方式、参数说明、示例命令、常见问题和文件说明。

### 覆盖内容

- 项目用途。
- Python 和 BBDown 环境要求。
- BBDown 登录说明。
- Python 跨平台脚本使用方式。
- 收藏夹批量下载示例。
- 单个视频下载示例。
- Windows CMD 多行写法和 `More?` 说明。
- Linux/macOS 示例。
- 音频模式和视频模式区别。
- 单元测试运行方式。
- 常见问题：收藏夹获取失败、下载目录为空、视频失败跳过、中文路径乱码、找不到 BBDown。
- 项目文件说明。

### 使用价值

后续用户无需查看源码即可根据 README 完成安装检查、登录、收藏夹下载、单个视频下载和问题排查。
