# 问题总和文档

## BBDown 下载目录写错位置问题

### 修复方式

统一使用 Python 脚本或 Tkinter GUI，并直接传递用户输入的原生路径，避免 `/g/...` 被 .NET 程序误识别。

## Windows PowerShell 脚本编码和语法验证问题

### 修复方式

PowerShell 版本脚本已删除，当前项目只维护 Python/uv 环境。

## Python 类型标注兼容旧版解释器问题

### 修复方式

旧版 Python 中将类型标注改为 `typing.List`。当前 uv 环境使用 Python 3.13，但核心脚本仍尽量保持兼容写法。

## GUI 不包含 BBDown 的运行依赖问题

### 修复方式

GUI 支持自动检测和手动选择 `bbdown.exe`，命令行脚本支持 `--bbdown-path`。

## GUI 日志中文乱码问题

### 修复方式

使用 `locale.getpreferredencoding(False)` 读取 BBDown 输出，并保留 `errors='replace'`。

## GUI 下载时反复弹出 CMD 窗口问题

### 修复方式

Windows 下调用 BBDown 时使用 `subprocess.STARTUPINFO`、`STARTF_USESHOWWINDOW` 和 `SW_HIDE` 隐藏子进程窗口。

## GUI 缺少停止下载能力问题

### 修复方式

GUI 增加停止任务按钮，核心下载逻辑增加 `should_stop` 回调和 `cancelled` 状态。

## 全局 Python 环境不利于复现问题

### 修复方式

新增 uv 项目配置，通过 `uv sync` 创建项目 `.venv`，通过 `uv run` 运行测试和 GUI。

## Faster-Whisper 合并后的 exe 打包风险

### 当前策略

先完成源码 GUI 合并，并用 `uv run python bilibili_downloader_ui.py` 验证转文字功能。确认稳定后，再单独处理包含 Faster-Whisper 的 exe 打包。

## 转文字批量转换中单个文件失败和空文本问题

### 修复方式

1. 新增 `probe_media_file()`，转录前用 `ffprobe` 校验媒体文件是否可解析且存在音频流。
2. 如果媒体文件损坏，直接返回失败，日志显示 `媒体文件无效` 和 ffprobe 原因。
3. 如果 faster-whisper 没有输出任何非空文本，不再保留空 `.txt`，并返回失败原因：`未识别到可写入的文字内容`。
4. 批量任务继续处理其他文件，并在统计中列出失败项。

## 视频转文字能力边界问题

### 问题说明

用户询问“视频转文字支持吗”。当前支持 `.mp4` 视频转文字，但本质是识别视频文件中的音频流。

### 能力边界

- 支持有音频流的 `.mp4`。
- 不支持识别视频画面中的文字。
- 不支持 OCR。
- 如果视频没有音频流，会提示 `媒体文件中没有可识别的音频流`。
- 如果视频损坏，会提示 `媒体文件无效`。

### 后续规范

README 和 GUI 说明中必须明确“视频转文字=识别视频中的声音”，避免用户误解为画面文字识别。
