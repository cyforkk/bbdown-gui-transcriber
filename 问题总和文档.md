# 问题总和文档

## BBDown 下载目录写错位置问题

### 问题现象

用户指定下载目录为：

```text
G:\默认收藏夹\音频
```

但下载完成后，用户在该目录中没有看到音频文件。

### 修复方式

后续统一使用 Python 脚本或 Tkinter GUI，并直接传递用户输入的原生路径，避免 `/g/...` 被 .NET 程序误识别。

## Windows PowerShell 脚本编码和语法验证问题

### 修复方式

该问题随 PowerShell 版本脚本删除而不再影响当前项目。

## Python 类型标注兼容旧版解释器问题

### 修复方式

将类型标注改为兼容旧版 Python 的 `typing.List`。

## GUI 不包含 BBDown 的运行依赖问题

### 修复方式

1. GUI 启动时默认尝试通过 PATH 自动检测 `bbdown`。
2. GUI 提供 `自动检测` 按钮。
3. GUI 提供 `选择 bbdown` 按钮，允许用户手动选择 `bbdown.exe`。
4. 命令行脚本新增 `--bbdown-path` 参数，用于显式指定 BBDown 可执行文件路径。

## GUI 日志中文乱码问题

### 修复方式

1. 新增 `get_subprocess_output_encoding()`，使用 `locale.getpreferredencoding(False)` 获取系统首选编码。
2. 当系统未返回编码时回退到 `utf-8`。
3. GUI 调用 BBDown 时使用该编码读取 stdout。
4. GUI 日志中输出当前使用的日志编码，方便排查。

## PyInstaller 打包环境缺失问题

### 修复方式

执行：

```bash
python -m pip install pyinstaller
```

安装完成后使用 `build_exe.py` 成功生成 exe。

## 目录整理后的临时产物清理

### 处理方式

已删除可再生成的临时产物，并新增 `release/BilibiliDownloaderUI.exe` 作为发布文件。

## GUI 下载时反复弹出 CMD 窗口问题

### 修复方式

1. 新增 `get_hidden_subprocess_kwargs()`。
2. Windows 下创建 `subprocess.STARTUPINFO()`。
3. 设置 `STARTF_USESHOWWINDOW` 和 `SW_HIDE`。
4. 调用 `subprocess.Popen` 时传入 `startupinfo`。
5. 非 Windows 平台返回空参数，不影响跨平台运行。

## GUI 缺少停止下载能力问题

### 问题现象

用户发现下载错了视频时，只能等待当前任务结束，无法从 GUI 主动停止下载。收藏夹批量下载时会继续下载后续视频。

### 修复方式

1. 核心下载结果新增 `cancelled` 状态。
2. 核心下载逻辑新增 `should_stop` 回调。
3. GUI 新增 `停止下载` 按钮。
4. 点击停止后设置 `stop_requested=True`。
5. 如果当前 BBDown 子进程仍在运行，调用 `terminate()` 终止当前进程。
6. 停止后不再继续下载收藏夹中的后续视频。
7. GUI 恢复开始按钮，状态显示 `已停止`。

### 后续规范

GUI 的长时间任务必须提供停止入口。停止时既要终止当前外部进程，也要阻止任务循环进入下一项。
