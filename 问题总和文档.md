# 问题总和文档

## BBDown 下载目录写错位置问题

### 问题现象

用户指定下载目录为：

```text
G:\默认收藏夹\音频
```

但下载完成后，用户在该目录中没有看到音频文件。

### 原因

执行命令时使用了 Bash 风格路径：

```text
/g/默认收藏夹/音频
```

BBDown 是 .NET 程序，在 Windows 环境下接收该路径后没有按预期映射到 `G:` 盘，而是写入到了错误位置：

```text
\g\默认收藏夹\音频
```

### 修复方式

1. 检查真实写入位置，确认音频文件位于 `\g\默认收藏夹\音频`。
2. 将已经下载的 `.m4a` 文件复制到正确目录 `G:\默认收藏夹\音频`。
3. 后续统一使用 Python 脚本，并直接传递用户输入的原生路径，避免 `/g/...` 被 .NET 程序误识别。

### 后续规范

在 Windows 上调用 BBDown、.NET CLI 或其他原生 Windows 程序时，下载目录必须使用 Windows 原生路径，例如：

```text
G:\默认收藏夹\音频
```

不要使用 Bash 风格路径：

```text
/g/默认收藏夹/音频
```

当前项目只维护 Python 跨平台脚本，不再维护 PowerShell 版本。

## Windows PowerShell 脚本编码和语法验证问题

### 问题现象

PowerShell 脚本第一次语法验证时出现解析错误，错误信息中包含字符串解析异常和 `&` 符号解析异常。

### 原因

1. 从 Bash 调用 PowerShell 时，`$null` 等 PowerShell 变量如果没有正确转义，会被 Bash 提前处理。
2. Windows PowerShell 5.1 对 UTF-8 文件编码较敏感，中文内容和字符串拼接不当时容易出现解析异常。
3. API URL 里直接写多个 `&` 参数时，如果字符串边界异常，会导致 PowerShell 把 `&` 当作调用运算符。

### 修复方式

该问题随 PowerShell 版本脚本删除而不再影响当前项目。当前保留该记录用于说明历史原因。

### 后续规范

当前项目只维护 Python 版本，避免再引入 PowerShell 编码和执行策略问题。

## Python 类型标注兼容旧版解释器问题

### 问题现象

运行 Python 单元测试时报错：

```text
TypeError: 'type' object is not subscriptable
```

### 原因

当前系统 Python 版本不支持 Python 3.9 引入的内置泛型写法，例如：

```python
list[FavoriteVideo]
```

### 修复方式

将类型标注改为兼容旧版 Python 的 `typing.List`：

```python
from typing import List

successes: List[FavoriteVideo]
```

### 后续规范

为了提高脚本跨平台兼容性，Python 脚本避免使用过新的语法特性，优先使用旧版 Python 也能运行的标准库写法。
